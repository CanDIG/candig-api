from ..beacon.request.handlers import collection_handler

async def datasets_search(body: dict):
    # Figure out what kind of search we should be doing
    # Pass out the parsed search parameters to SQL
    # Fill out the return value with all parameters that belong there
    
    pass